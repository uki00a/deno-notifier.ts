# generated by octocov init
codeToTestRatio:
  code:
    - '**/*.ts'
    - '!**/*_test.ts'
  test:
    - '**/*_test.ts'
coverage:
  paths:
    - coverage/lcov.info
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
repository: uki00a/deno-notifier.ts
